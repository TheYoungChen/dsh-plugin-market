# Changelog

本项目的版本历史与显著变更。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-08-14

首个正式发布：插件市场 bundle，侧边栏「设置」上方 + 设置 → 插件 → 插件市场 双入口。

### Added

- **双入口**：侧边栏入口按钮 + 设置页「插件市场」标签页，共享同一套内容
- **全量聚合**：按 star 区间分页抓取 GitHub `dsh-plugin` topic 的**全部**仓库（突破单查询 1000 条上限），按 star / 最新更新时间排序
- **静态索引**：`registry.json` 由 GitHub Action 每 2 小时生成、jsDelivr CDN 分发，终端零 API 调用、零限流；搜索 API 兜底
- **类型识别 + 徽章**：按仓库根文件识别 插件 / Skill / 预设 / 脚本，卡片上显示彩色徽章
- **多类型安装**：
  - 插件 → `pnpm add`（官方路径，自动 reconcile bundles）
  - Skill → 克隆到 `~/.dsh/skills/<名>/`
  - 预设 → 克隆到 `~/.dsh/.agent-presets/<名>/`
  - 脚本 → 克隆后执行安装脚本（含信任确认）
- **安装体验**：确认框 → 实时日志 + 已用时长 → 随时**终止** / 转**后台下载**；右上角常驻 toast，可点击展开为终端面板，完成后「立即重启」
- **已安装识别 + 更新**：读 profile 真实依赖 + `repository` 归属校验，已装插件置顶；自动对比最新版本，有新版显示「更新」按钮
- **筛选与排序**：分类（全部/插件/Skill/预设/脚本）+ 排序（最受欢迎/最新）+ 关键字搜索 + 刷新
- **统计与指引**：插件总数与分类计数、「如何发布插件」引导

### Fixed

- 数据源无超时导致列表在部分网络下无法加载（为每个数据源加硬超时 + 搜索 API 兜底）

### Notes

- GitHub 搜索单查询上限 1000 条：本索引按 star 区间分页规避，覆盖全部仓库
- 安装的第三方插件需自查源码与许可证；本市场只提供发现与安装入口
